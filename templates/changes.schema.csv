# Reference for the change-log CSVs the scripts generate automatically.
# These logs are the reversible record of every operation and are NOT committed
# (see .gitignore). To undo an operation, move dest -> source for rows with
# status=ok. See docs/07-verify-and-delete.md.
#
# reorg_move.py  ->  changes_reorg_YYYYMMDD.csv
date;group;source;dest;n_src_before;n_dst_added;n_src_after;status;mode
#
# evacuate.py    ->  changes_evacuation_YYYYMMDD.csv
date;group;pass;source;dest;n_source;n_dest;status;mode
